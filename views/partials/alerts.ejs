<% if (alerts && alerts.error) { %>
    <div class="card red">
        <div class="card-content" style="padding: 10px">
            <% alerts.error.forEach(e => { %>
                <p>Error: <%= e  %></p>
                <% }) %>
            </div>
        </div>
    
<% } %> 